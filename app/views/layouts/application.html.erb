<!DOCTYPE html>
<html>
  <head>
    <title>ExpenseEase</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="font-sans">
    <header>
      <% if notice %>
        <p class="bg-gray-400 px-2 py-4 mx-auto font-sans font-medium text-center text-white"><%= notice %></p>
      <% end %>

      <% if alert %>
        <p class="bg-gray-600 px-2 py-4 mx-auto font-sans font-medium text-center text-white"><%= alert %></p>
      <% end %>
    </header>

    <% if user_signed_in? && !( params[:controller] == 'devise/registrations' && params[:action] == 'edit' ) %>
      <%= render "shared/navbar" %>
    <% end %>

    <%= yield %>

    <%= turbo_frame_tag :remote_modal %>
  </body>
</html>
